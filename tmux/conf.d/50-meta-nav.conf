# --- No-prefix navigation (Meta/Option) ---
# In Terminal.app, enable: Profiles > Keyboard > "Use Option as Meta key"
bind -n M-, select-pane -t :.-
bind -n M-. select-pane -t :.+
bind -n M-< previous-window
bind -n M-> next-window
